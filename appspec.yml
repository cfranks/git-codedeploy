version: 0.0
os: linux
files:
  - source: /
    destination: /var/www
permissions:
  - object: /var/www
    pattern: "*"
    owner: www-data
    group: www-data
    mode: 777
    type:
      - directory
